<UserControl x:Class="Undy.Views.SalesOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Undy.Views"
             mc:Ignorable="d" 
             d:DesignWidth="1980" d:DesignHeight="1048">
    <Grid>
        <Border Style="{DynamicResource Card}">
            <DataGrid Style="{DynamicResource DataGrid.Table}" ItemsSource="{Binding SaleView}" MaxHeight="1000" IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ordrenummer" Binding="{Binding OrderNumber}" Width="*" />
                    <DataGridTemplateColumn Header="Ordrestatus" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OrderStatus}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox SelectedItem="{Binding OrderStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          ItemsSource="{Binding DataContext.OrderStatusOptions, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Betalingsstatus" Binding="{Binding PaymentStatus}" Width="*" />
                    <DataGridTextColumn Header="Salgsdato" Binding="{Binding SalesDate, StringFormat=\{0:dd-MM-yyyy\}}" Width="*" />
                    <DataGridTextColumn Header="Pris" Binding="{Binding TotalPrice, StringFormat=\{0:C\}}" Width="*" />
                    <DataGridTextColumn Header="Produktnavn" Binding="{Binding ProductName}" Width="*" />
                    <DataGridTextColumn Header="Antal" Binding="{Binding Quantity}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
