<UserControl x:Class="Undy.Views.SalesOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1280" d:DesignHeight="720">

    <Border Background="{DynamicResource Brush.Background}"
            Padding="20">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="560"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- LEFT: Sales orders list -->
            <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="0">
                <TextBlock Text="Sales Orders"
                           FontSize="26"
                           FontWeight="Bold"
                           Margin="0,0,0,12"
                           Foreground="{DynamicResource Brush.Text}" />

                <ListView x:Name="SalesListView"
                          ItemsSource="{Binding SalesOrders}"
                          SelectedItem="{Binding SelectedSalesOrder}"
                          Style="{StaticResource ListView.Table}"
                          Height="720"
                          MouseDoubleClick="SalesListView_MouseDoubleClick"
                          SelectionMode="Single">

                    <ListView.View>
                        <GridView>

                            <GridViewColumn Header="Order #"
                                            DisplayMemberBinding="{Binding SalesOrderNumber}"
                                            Width="110"/>

                            <GridViewColumn Header="Status"
                                            DisplayMemberBinding="{Binding OrderStatus}"
                                            Width="150"/>

                            <GridViewColumn Header="Total"
                                            DisplayMemberBinding="{Binding TotalPrice}"
                                            Width="130"/>

                            <GridViewColumn Header="Customer"
                                            DisplayMemberBinding="{Binding CustomerName}"
                                            Width="240"/>

                        </GridView>
                    </ListView.View>

                </ListView>
            </StackPanel>

            <!-- RIGHT: Selected order details -->
            <StackPanel Grid.Row="0" Grid.Column="2">
                <TextBlock Text="Selected Order Details"
                           FontSize="26"
                           FontWeight="Bold"
                           Margin="0,0,0,12"
                           Foreground="{DynamicResource Brush.Text}" />

                <Border Background="{DynamicResource Brush.Panel}"
                        CornerRadius="10"
                        Padding="14"
                        Margin="0,0,0,16">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Order Number:" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedSalesOrder.SalesOrderNumber}" Foreground="{DynamicResource Brush.Text}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="E-Mail:" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedSalesOrder.CustomerEmail}" Foreground="{DynamicResource Brush.Text}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Order Date:" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedSalesOrder.SalesDate}" Foreground="{DynamicResource Brush.Text}" />

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Shipped Date:" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedSalesOrder.ShippedDate}" Foreground="{DynamicResource Brush.Text}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Total Price:" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedSalesOrder.TotalPrice}" Foreground="{DynamicResource Brush.Text}" />

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Customer ID:" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}" />
                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedSalesOrder.CustomerNumber}" Foreground="{DynamicResource Brush.Text}" />
                    </Grid>

                </Border>
            </StackPanel>

            <!-- RIGHT: Order lines -->
            <Grid Grid.Row="1" Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Order Lines"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0,0,0,10"
                           Foreground="{DynamicResource Brush.Text}" />

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding SelectedOrderLines}"
                          AutoGenerateColumns="False"
                          Style="{StaticResource DataGrid.SalesOrder}"
                          IsReadOnly="True"
                          CanUserReorderColumns="False"
                          CanUserResizeColumns="False">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*" />
                        <DataGridTextColumn Header="Quantity"     Binding="{Binding Quantity}"    Width="120" />
                        <DataGridTextColumn Header="Unit Price"   Binding="{Binding UnitPrice}"   Width="140" />
                        <DataGridTextColumn Header="Line Total"   Binding="{Binding LineTotal}"   Width="160" />
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>

        </Grid>
    </Border>
</UserControl>
