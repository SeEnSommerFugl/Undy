<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- BRUSHES -->
    <SolidColorBrush x:Key="Brush.TableRowAlt" Color="#0AFFFFFF" />
    <SolidColorBrush x:Key="Brush.TableHover" Color="#12FFFFFF" />
    <SolidColorBrush x:Key="Brush.TableSelected" Color="#22FFFFFF" />
    <SolidColorBrush x:Key="Brush.TableHeader" Color="#11000000" />
    <SolidColorBrush x:Key="Brush.TableHeaderBorder" Color="#33000000" />
    <SolidColorBrush x:Key="Brush.Text" Color="White" />
    <SolidColorBrush x:Key="Brush.TextSelected" Color="White" />


    <!-- LISTVIEW ITEM STYLE (ingen Template!) -->
    <Style x:Key="ListViewItem.Table" TargetType="ListViewItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="Padding" Value="12,14"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="SemiBold"/>


        <Style.Triggers>
            <!-- Hover -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background"
                        Value="{StaticResource Brush.TableHover}" />
            </Trigger>

            <!-- Selected -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background"
                        Value="{StaticResource Brush.TableSelected}" />
                <Setter Property="Foreground"
                        Value="{StaticResource Brush.TextSelected}" />
            </Trigger>

            <!-- Alternating row -->
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background"
                        Value="{StaticResource Brush.TableRowAlt}" />
            </Trigger>
        </Style.Triggers>
    </Style>


    <!-- LISTVIEW – TABLE STYLE -->
    <Style x:Key="ListView.Table" TargetType="ListView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>

        <!-- Alternating rows kræver AlternationCount -->
        <Setter Property="AlternationCount" Value="2"/>

        <!-- Brug vores ListViewItem-style -->
        <Setter Property="ItemContainerStyle"
                Value="{StaticResource ListViewItem.Table}" />

        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- GRIDVIEW COLUMN HEADER STYLE  -->
    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="{StaticResource Brush.TableHeader}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="Padding" Value="8,10" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.TableHeaderBorder}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>







</ResourceDictionary>
