<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============ NAVIGATION (ListBox-baseret) ============ -->

    <!-- Hvad der står på “knappen” -->
    <DataTemplate x:Key="NavItemTemplate">
        <TextBlock Text="{Binding Title}"
               Foreground="White"
               FontSize="20"
               FontWeight="SemiBold"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>
    </DataTemplate>

    <!-- Hver post opfører sig som en rund “knap” -->
    <Style x:Key="NavListBoxItem" TargetType="ListBoxItem">
        <!-- flise-størrelse/margins som dine knapper -->
        <Setter Property="Height" Value="40"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Card"
                        CornerRadius="15"
                        Background="{DynamicResource Brush.Accent}"
                        BorderBrush="{DynamicResource Brush.Accent}"
                        BorderThickness="1"
                        SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover som dine knapper -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Card" Property="Background"  Value="{DynamicResource Brush.AccentHover}"/>
                            <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource Brush.AccentHover}"/>
                        </Trigger>
                        <!-- Valgt = grøn -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Card" Property="Background"  Value="{DynamicResource Amount.Positive.Bg}"/>
                            <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource Amount.Positive.Bg}"/>
                        </Trigger>
                        <!-- Disabled-look -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Card" Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Selve ListBox’en (rammeløs, ingen scrollbar i venstre rail) -->
    <Style x:Key="NavListBox" TargetType="ListBox">
        <Setter Property="ItemTemplate" Value="{DynamicResource NavItemTemplate}"/>
        <Setter Property="ItemContainerStyle" Value="{DynamicResource NavListBoxItem}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="{x:Null}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
    </Style>



</ResourceDictionary>