<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =================== CHIPS (for detail panels) =================== -->
    <Style x:Key="ChipText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Amount.Neutral.Fg}"/>
        <Setter Property="FontSize" Value="40"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="ChipPositive" TargetType="Border">
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Background" Value="{DynamicResource Amount.Positive.Bg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Amount.Positive.Bg}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="ChipNegative" TargetType="Border">
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Background" Value="{DynamicResource Amount.Negative.Bg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Amount.Negative.Bg}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

</ResourceDictionary>